[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "colorama_rs"
version = "0.1.0"
description = "Fast Rust implementation of Python's colorama library"
requires-python = ">=3.8"

[tool.maturin]
features = ["pyo3/extension-module"]
